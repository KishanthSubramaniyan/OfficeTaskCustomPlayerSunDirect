<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="viewModels"
            type="com.sundirect.customplayer.presentationLayer.viewmodels.HomeScreenViewModels" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/imgHero"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:showCarouselImage="@{viewModels.homeListImage.thumbnail}"/>

        <!--<androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            android:scrollbars="horizontal"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:adapter="@{viewModels.carouselImageAdapterView}"
            app:moveAdapterImage="@{viewModels.index}"
            app:carouselAdapter="@{viewModels.heroList}"/>-->

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>